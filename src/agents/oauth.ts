/**
 * OAuth module — REMOVED.
 *
 * Zaakify now uses Z.AI (Zhipu GLM) with simple API key auth.
 * No OAuth, no token exchange, no stealth headers.
 *
 * This file is kept as a stub to avoid import errors during cleanup.
 */

// No exports needed — this module is no longer used.
